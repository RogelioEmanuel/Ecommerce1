@{ 
    string num = @Session["itemTotal"].ToString();

}
@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()
        //string namee = @Session["name"].ToString();
        <nav id="navbar" class="navbar">
<ul>@*<ul class="nav navbar-nav navbar-right">*@
    <li>
        <a class="btn" href="@Url.Action("Index", "Carro")">

            <i class="bi bi-cart" style="font-size:24px;color:blue"></i>
            <span class="badge" style="color:black">@num</span>
        </a>
        @*@Html.ActionLink("Hola " + "namee" + " ", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })*@
    </li>
    <li class="dropdown">
        <a href="#"><span>Bienvenido</span> <i class="bi bi-chevron-down"></i></a>
        <ul>
            <li>@Html.ActionLink("Mis Pedidos", "Index", "Pedidos", null, new { id = "add" })</li>
            @*<li>@Html.ActionLink("Ver todas", "Index", "Clientes", null, new { id = "list" })</li>*@
        </ul>
    </li>
    @*<li>
        <a class="dropdown-toggle" role="button" aria-expanded="false" href="#" data-toggle="dropdown">Hola namees<span class="caret"></span></a>
        <ul class="dropdown-menu" role="menu">
            <li>@Html.ActionLink("Mis Pedidos", "Index", "Pedidos", null, new { id = "add" })</li>
        </ul>
    </li>*@
    
    <li><a href="javascript:document.getElementById('logoutForm').submit()">Cerrar sesión</a></li>
</ul>
            </nav>
    }
}
else
{
    <!--<ul class="nav navbar-nav navbar-right">-->
        <li>
            <a class="btn" href="@Url.Action("Index", "Carro")">
                <i class="bi bi-cart" style="font-size:24px;color:blue"></i>
                <span class="badge" style="color:black">@num</span>
            </a>
        </li>

        <li>@Html.ActionLink("Registrarse", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Iniciar sesión", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    <!--</ul>-->
}
 